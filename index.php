<?php require_once __DIR__ . '/app/auth.php'; require_login(); require_once __DIR__ . '/app/layout.php'; $pdo=db(); $s=app_stats($pdo); $due=$pdo->query("SELECT c.*,p.name package_name,p.price FROM customers c LEFT JOIN packages p ON p.id=c.package_id ORDER BY c.due_day ASC,c.name ASC LIMIT 8")->fetchAll(); $last=$pdo->query("SELECT pay.*,c.name customer_name FROM payments pay JOIN customers c ON c.id=pay.customer_id ORDER BY pay.paid_at DESC,pay.id DESC LIMIT 8")->fetchAll(); render_header('Dashboard Billing'); ?>
<section class="hero-strip"><div class="hero-card"><p class="font-black uppercase tracking-[.18em] text-white/75 text-xs mb-2">Billing siap operasional</p><h2>Kelola pelanggan internet lebih cepat, terang, dan nyaman dipakai tiap hari.</h2><p class="text-white/85">Pantau tagihan, pelanggan ON/OFF, dan pemasukan tanpa tampilan kaku.</p></div><div class="quick-card"><p class="eyebrow">Aksi cepat</p><h2 class="text-2xl font-black tracking-tight mb-3">Mulai kerja</h2><div class="flex flex-wrap gap-2"><a class="btn primary" href="customers.php?action=new">+ Pelanggan</a><a class="btn soft" href="payments.php?action=new">+ Pembayaran</a></div></div></section>
<section class="cards"><div class="card metric" style="--glow:#dbeafe"><small>Total Pelanggan</small><strong><?=$s['total']?></strong><span class="pill blue">Database</span></div><div class="card metric" style="--glow:#dcfce7"><small>Pelanggan ON</small><strong><?=$s['on']?></strong><span class="pill green">Aktif</span></div><div class="card metric" style="--glow:#ffedd5"><small>Belum Lunas</small><strong><?=$s['unpaid']?></strong><span class="pill orange">Follow up</span></div><div class="card metric" style="--glow:#fce7f3"><small>Pemasukan Bulan Ini</small><strong><?=rupiah($s['monthIncome'])?></strong><span class="pill blue"><?=e($s['month'])?></span></div></section>
<section class="grid-2"><div class="card"><div class="panel-title"><h2>Pelanggan & Jatuh Tempo</h2><a class="btn soft" href="customers.php">Lihat semua</a></div><div class="table-wrap"><table class="responsive"><thead><tr><th>ID</th><th>Nama</th><th>Paket</th><th>Jatuh Tempo</th><th>Status</th></tr></thead><tbody><?php foreach($due as $c):?><tr><td data-label="ID"><b><?=e($c['customer_code'])?></b></td><td data-label="Nama"><?=e($c['name'])?></td><td data-label="Paket"><?=e($c['package_name'] ?? '-')?></td><td data-label="Tempo">Tgl <?=e($c['due_day'])?></td><td data-label="Status"><span class="pill <?=$c['is_active']?'green':'red'?>"><?=$c['is_active']?'ON':'OFF'?></span></td></tr><?php endforeach;if(!$due):?><tr><td colspan="5">Belum ada pelanggan.</td></tr><?php endif;?></tbody></table></div></div><div class="card"><div class="panel-title"><h2>Pembayaran Terakhir</h2><a class="btn soft" href="payments.php">Detail</a></div><div class="table-wrap"><table class="responsive"><thead><tr><th>Tanggal</th><th>Pelanggan</th><th>Nominal</th></tr></thead><tbody><?php foreach($last as $p):?><tr><td data-label="Tanggal"><?=e($p['paid_at'])?></td><td data-label="Pelanggan"><?=e($p['customer_name'])?></td><td data-label="Nominal"><b><?=rupiah($p['amount'])?></b></td></tr><?php endforeach;if(!$last):?><tr><td colspan="3">Belum ada pembayaran.</td></tr><?php endif;?></tbody></table></div></div></section>
<?php render_footer(); ?>
